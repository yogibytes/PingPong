1) go to the root dir and run following cmds from the cli
   - npm install ws
2) comiple the file by running the
   - tsc
3) then run the js file in the /dist
   - node server.js
4) use postman or any other tool to connect to "ws://localhost:8080" and send "ping" or "pong"
   you recieve opposite !
